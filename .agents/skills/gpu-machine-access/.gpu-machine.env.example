# Required: SSH config alias, hostname, IP address, or user@host.
GPU_MACHINE_SSH_TARGET=

# Optional connection and working-directory settings.
GPU_MACHINE_SSH_PORT=
GPU_MACHINE_REMOTE_DIR=

# Optional fallback. Prefer an SSH key and keep real passwords only in .gpu-machine.env.
GPU_MACHINE_SSH_PASSWORD=
